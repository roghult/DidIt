import com.didit.module
import io.ktor.http.*
import io.ktor.server.testing.*
import org.junit.Test
import kotlin.test.assertEquals

class HabitRouteTests {

    // @Test
    // fun `test GET habit without any habits returns empty list and OK status`() {
    //     withTestApplication({ module(testing = true) }) {
    //         handleRequest(HttpMethod.Get, "/habit").apply {
    //             assertEquals(
    //                 """[]""",
    //                 response.content
    //             )
    //             assertEquals(HttpStatusCode.OK, response.status())
    //         }
    //     }
    // }
}